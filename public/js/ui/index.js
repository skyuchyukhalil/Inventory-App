import { renderToolList } from './renderToolList.js';
import { renderModalUI } from './renderModal.js'; // Ensure filename matches
import { renderAddToolForm } from './renderAddForm.js';
import { renderLoadingState } from './renderLoading.js';

export const UI = {
    renderToolList,
    renderModalUI,
    renderAddToolForm,
    renderLoadingState
};